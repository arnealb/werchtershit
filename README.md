# Rock Werchter 2026 — Lineup Planner

A full-stack web app for planning your Rock Werchter 2026 experience. Browse the festival timetable, select artists, and generate a Spotify playlist with tracks from those artists.

## Features

- **Timetable UI** — pixel-accurate timeline grid per day and stage
- **Artist selection** — click to select/deselect, select all per day
- **Spotify integration** — OAuth login, playlist preview, one-click playlist creation
- **Artist matching** — searches Spotify by selected festival artist name
- **Debug panel** — inspect matched/unmatched artists and track counts

## Setup

### 1. Install dependencies

```bash
npm install
```

### 2. Configure Spotify

1. Go to [Spotify Developer Dashboard](https://developer.spotify.com/dashboard)
2. Create a new app
3. Add `http://localhost:3000/api/spotify/callback` as a **Redirect URI**
4. Copy the **Client ID** and **Client Secret**

```bash
cp .env.local.example .env.local
# Edit .env.local with your Spotify credentials
```

### 3. Fetch the lineup data

The app scrapes the official Rock Werchter schedule pages. Run this once to populate the cache:

```bash
npm run fetch-lineup
```

This saves `data/lineup.json`. The app uses this file and won't re-scrape on every load.
If the site changes, just re-run this command.

### 4. Start the app

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000)

---

## Deployment (Vercel + Supabase)

Vercel's filesystem is read-only, so production uses Supabase instead of `data/lineup.json`:

- **`lineup_cache`** — shared lineup data (replaces the JSON file)
- **`user_selections`** — per-user artist selections, keyed by Spotify user id (multi-user)

Locally, Supabase is optional: without `SUPABASE_URL` the app falls back to `data/lineup.json`
and selection persistence is simply disabled.

### 1. Create a Supabase project

1. Go to [supabase.com](https://supabase.com) → New project
2. Open **SQL Editor** and run the contents of [`supabase/schema.sql`](./supabase/schema.sql)
3. Copy from **Project Settings → API**: the **Project URL** and the **`service_role` key**

### 2. Seed the lineup

Add the Supabase credentials to `.env.local`, then:

```bash
npm run seed-lineup
```

This uploads `data/lineup.json` to the `lineup_cache` table so production never needs to scrape on first load.

### 3. Deploy on Vercel

1. Push this repo to GitHub
2. [vercel.com/new](https://vercel.com/new) → import the repo
3. Set the environment variables (see `.env.local.example`):
   - `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET`
   - `SPOTIFY_REDIRECT_URI` = `https://<your-app>.vercel.app/api/spotify/callback`
   - `OPENAI_API_KEY`
   - `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
4. Deploy

### 4. Update Spotify

In the [Spotify Developer Dashboard](https://developer.spotify.com/dashboard), add the production
callback URL (`https://<your-app>.vercel.app/api/spotify/callback`) as a Redirect URI.

> **Note:** new Spotify apps run in *Development Mode* — only users you explicitly add under
> **User Management** can log in. Add your testers' Spotify emails there.

---

## How it works

### Timetable
- 4 days: Thursday–Sunday (2–5 July 2026)
- 4 stages per day: Main Stage, The Barn, KluB C, The Slope
- Artist blocks are positioned using CSS `left`/`width` based on minutes
- Horizontal scroll for the full day timeline
- Stage name column is sticky on the left

### Spotify playlist
- For each selected artist, it searches Spotify for tracks by artist name
- You can choose max tracks per artist (1–10 or all)
- Creates a new private playlist in your Spotify account

### Artist name matching
Matching handles:
- Case differences
- Diacritics (é → e)
- Punctuation (feat., ft., &)
- Partial name overlap (e.g. "fred again.." ↔ "Fred Again")

### Re-scraping
```bash
# Re-fetch lineup from Rock Werchter website
npm run fetch-lineup

# Or trigger via API (POST)
curl -X POST http://localhost:3000/api/lineup
```

---

## Project structure

```
src/
  app/
    planner/page.tsx          # Main server component
    api/lineup/route.ts       # Lineup data API
    api/spotify/              # Auth, callback, playlist, create
  components/
    timetable/                # Grid UI components
    PlannerClient.tsx         # Main interactive client component
    SelectedArtistsPanel.tsx
    PlaylistPreview.tsx
    DebugPanel.tsx
  lib/
    scraper.ts                # Rock Werchter HTML scraper
    lineup.ts                 # Read/write JSON cache
    matcher.ts                # Artist name matching
    spotify.ts                # Spotify Web API client
  types/
    lineup.ts                 # Festival data types
    spotify.ts                # Spotify types
data/
  lineup.json                 # Cached lineup (generated by fetch-lineup)
scripts/
  fetch-lineup.ts             # Scrape and cache lineup data
```
# werchtertimetableshit
# werchtershit

